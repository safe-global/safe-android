<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pm.gnosis.heimdall">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="pm.gnosis.heimdall.GnosisAuthenticatorApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pm.gnosis.heimdall.ui.splash.SplashActivity"
            android:launchMode="singleTask"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pm.gnosis.heimdall.ui.onboarding.GenerateMnemonicActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name="pm.gnosis.heimdall.ui.MainActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="pm.gnosis.heimdall.ui.transactiondetails.TransactionDetailsActivity"
            android:parentActivityName="pm.gnosis.heimdall.ui.MainActivity" />
    </application>
</manifest>
